# Add the following to this file to add Firebase:
# ============================================
# 1. Download GoogleService-Info.plist from Firebase Console
# 2. Drag it into ios/Runner/ in Xcode (DO NOT just copy the file)
# 3. Make sure "Copy if needed" is checked
# 4. Verify the file appears under Runner target

# GoogleService-Info.plist is NOT tracked in git (add to .gitignore)
# For CI/CD, use environment variable GOOGLE_SERVICES_INFO_PLIST_BASE64
# and decode it in your pipeline:
#   echo $GOOGLE_SERVICES_INFO_PLIST_BASE64 | base64 -d > ios/Runner/GoogleService-Info.plist
